// README.md
# Playwright Test Structure (Enterprise Standard)

- All UI (end-to-end) tests are in tests/ui
- All API tests are in tests/api
- Use page objects and fixtures for maintainability
- Keep test files atomic and independent
- Follow naming conventions: *.spec.js or *.spec.ts
