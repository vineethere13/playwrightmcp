# API Tests

This folder contains all API Playwright tests following enterprise framework standards.

- Use context and fixtures for API authentication and setup.
- Name test files with `.spec.js` or `.spec.ts`.
- Keep tests atomic and independent.
