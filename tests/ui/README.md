# UI Tests

This folder contains all UI (end-to-end) Playwright tests following enterprise framework standards.

- Use page objects for all page interactions.
- Use fixtures for login, dashboard, and other common flows.
- Name test files with `.spec.js` or `.spec.ts`.
- Keep tests atomic and independent.
